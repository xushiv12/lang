#include"wlang.h" 
A B C{
	if(msg==WM_CREATE){
		MessageBox(0,"CREATE WINDOW!","CREATE",MB_OK);
		return 0;
	}
	return P1;
}
int API n{
	W1=xushi;
	W2=h;
	W3="xu";
	R;
	HWND hwnd=CreateWindow(
	"xu",
	"A window",
	WS_IT,
	1,1,3000,3000,
	0,0,h,0
	);
	ShowWindow(hwnd,bbb);
	MSG msg;
	run(msg);
	return 0;
}
